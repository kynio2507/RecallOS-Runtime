{
  "name": "recallos-runtime",
  "version": "1.0.0-local",
  "description": "RecallOS Runtime multi-module MCP/server tool platform with Code Intel, memory, diagnostics, and policy modules.",
  "type": "module",
  "private": true,
  "bin": {
    "recall": "./src/cli/recall.mjs",
    "recallos-runtime": "./src/recallos_runtime_mcp.mjs"
  },
  "scripts": {
    "start": "node ./src/cli/recall.mjs mcp",
    "mcp": "node ./src/recallos_runtime_mcp.mjs",
    "help": "node ./src/cli/recall.mjs --help",
    "test": "node ./test/test_recallos_runtime_mcp.mjs",
    "status": "node ./src/cli/recall.mjs modules"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.1",
    "better-sqlite3": "^12.10.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
